# UKCEH NC Labour Explorer — Exports fix
- Fixed PNG/PPT exports to use **plotly.io** (not `px.io`).
- Includes `kaleido` and `python-pptx` in requirements so image export works.
- First tab has radio toggle between the two split views.
